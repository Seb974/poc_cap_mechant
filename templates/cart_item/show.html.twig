{% extends 'base.html.twig' %}

{% block title %}CartItem{% endblock %}

{% block body %}
    <h1>CartItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cart_item.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ cart_item.quantity }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ cart_item.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cart_item_index') }}">back to list</a>

    <a href="{{ path('cart_item_edit', {'id': cart_item.id}) }}">edit</a>

    {{ include('cart_item/_delete_form.html.twig') }}
{% endblock %}
